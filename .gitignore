# Claude Code local settings
.claude/settings.local.json

# Windows junk
*.lnk
Thumbs.db
desktop.ini

# OS junk
.DS_Store

# If you ever add packaging/build output
web-ext-artifacts/
*.zip
*.xpi

# Node / web-ext tooling
node_modules/
package.json
package-lock.json

# web-ext runtime files
.amo-upload-uuid
web-ext-commands.txt

opusplan.bat
RELEASE_NOTES.md

# Dev scratch: saved pages used for ad-hoc perf/debugging (not shipped, not committed).
# Tests that consume these (e.g. tests/run-editable-mutation.js) degrade gracefully when absent.
examples/
reddit_entry.txt